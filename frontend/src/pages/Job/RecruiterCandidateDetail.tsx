import { useEffect, useState } from 'react';
import { ArrowLeft, ExternalLink, FileText, Mail, Phone, RefreshCw, User } from 'lucide-react';
import { useNavigate, useParams } from 'react-router-dom';
import api from '../../api/axios';
import { formatDate, getErrorMessage, getScoreColor, getStageColor, getStageLabel } from '../../utils/formatters';

interface Application { id: string; stage: string; matchingScore: number; skillScore: number; experienceScore: number; aiExplanation?: string | null; createdAt: string; job: { title: string; location: string; salaryRange?: string | null; company?: { name: string } }; candidateProfile: { skills: string[]; experience?: { years?: number; position?: string; summary?: string }; education?: { summary?: string; school?: string; major?: string }; resumeUrl?: string | null; user: { id: string; fullName?: string; email: string; phone?: string } } }

export default function RecruiterCandidateDetail() {
  const { candidateId } = useParams<{ candidateId: string }>();
  const navigate = useNavigate();
  const [applications, setApplications] = useState<Application[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');

  useEffect(() => { if (!candidateId) return; let active = true; api.get(`/application/recruiter/candidate/${candidateId}`).then((response) => { if (active) setApplications(Array.isArray(response.data) ? response.data : []); }).catch((requestError) => { if (active) setError(getErrorMessage(requestError)); }).finally(() => { if (active) setLoading(false); }); return () => { active = false; }; }, [candidateId]);
  if (loading) return <div className="flex min-h-[320px] items-center justify-center text-sm text-slate-500"><RefreshCw className="mr-3 h-5 w-5 animate-spin text-blue-500" />Đang tải hồ sơ ứng viên...</div>;
  if (error || applications.length === 0) return <div className="space-y-4"><button type="button" onClick={() => navigate('/recruiter/candidates')} className="inline-flex items-center gap-2 text-sm font-semibold text-blue-600"><ArrowLeft className="h-4 w-4" />Back to candidates</button><div className="rounded-xl border border-rose-200 bg-rose-50 p-5 text-sm text-rose-700">{error || 'Không tìm thấy ứng viên trong các công việc bạn quản lý.'}</div></div>;
  const first = applications[0]; const candidate = first.candidateProfile; const user = candidate.user;
  return <div className="mx-auto max-w-4xl space-y-5"><button type="button" onClick={() => navigate('/recruiter/candidates')} className="inline-flex items-center gap-2 text-sm font-semibold text-blue-600"><ArrowLeft className="h-4 w-4" />Back to candidates</button><section className="rounded-2xl bg-slate-900 p-6 text-white shadow-xl"><div className="flex items-center gap-4"><div className="grid h-16 w-16 place-items-center rounded-2xl bg-blue-600 text-2xl font-black">{(user.fullName || 'C').charAt(0).toUpperCase()}</div><div><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-wide text-blue-300"><User className="h-4 w-4" />Candidate profile</div><h1 className="mt-1 text-2xl font-black">{user.fullName || 'Unnamed candidate'}</h1><div className="mt-2 flex flex-wrap gap-3 text-sm text-slate-300"><span className="flex items-center gap-1"><Mail className="h-4 w-4" />{user.email}</span>{user.phone && <span className="flex items-center gap-1"><Phone className="h-4 w-4" />{user.phone}</span>}</div></div></div></section><div className="grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-bold text-slate-900">Professional profile</h2><p className="mt-3 text-sm text-slate-600">{candidate.experience?.position || 'Position not specified'} · {candidate.experience?.years ?? 0} years</p><p className="mt-3 whitespace-pre-line text-sm leading-6 text-slate-600">{candidate.experience?.summary || 'No experience summary provided.'}</p><h3 className="mt-5 font-semibold text-slate-900">Skills</h3><p className="mt-2 text-sm text-slate-600">{candidate.skills.join(', ') || 'No skills listed.'}</p><h3 className="mt-5 font-semibold text-slate-900">Education</h3><p className="mt-2 text-sm text-slate-600">{candidate.education?.summary || [candidate.education?.school, candidate.education?.major].filter(Boolean).join(' - ') || 'No education information.'}</p>{candidate.resumeUrl && <a href={candidate.resumeUrl} target="_blank" rel="noreferrer" className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-blue-600"><FileText className="h-4 w-4" />View resume <ExternalLink className="h-3.5 w-3.5" /></a>}</section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-bold text-slate-900">Applications</h2><div className="mt-4 space-y-3">{applications.map((application) => <div key={application.id} className="rounded-xl border border-slate-200 p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-slate-900">{application.job.title}</p><p className="mt-1 text-xs text-slate-500">{application.job.company?.name || 'Company'} · {application.job.location}</p><p className="mt-1 text-xs text-slate-400">Applied {formatDate(application.createdAt)}</p></div><span className={`rounded-full border px-2.5 py-1 text-xs font-semibold ${getStageColor(application.stage)}`}>{getStageLabel(application.stage)}</span></div><div className="mt-4 grid grid-cols-3 gap-2 text-center"><div><p className="text-xs text-slate-500">Match</p><p className={`font-bold ${getScoreColor(application.matchingScore)}`}>{Math.round(application.matchingScore)}%</p></div><div><p className="text-xs text-slate-500">Skills</p><p className="font-bold text-slate-800">{Math.round(application.skillScore)}%</p></div><div><p className="text-xs text-slate-500">Experience</p><p className="font-bold text-slate-800">{Math.round(application.experienceScore)}%</p></div></div>{application.aiExplanation && <p className="mt-3 rounded-lg bg-indigo-50 p-3 text-xs leading-5 text-indigo-800">{application.aiExplanation}</p>}</div>)}</div></section></div></div>;
}
